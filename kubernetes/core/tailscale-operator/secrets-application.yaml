apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tailscale-secrets
  namespace: argocd
  annotations:
    # Deploy secrets BEFORE the main application (sync-wave 5)
    argocd.argoproj.io/sync-wave: "1"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/LorenzoDeBie/Homelab-new.git
    targetRevision: main
    path: kubernetes/core/tailscale-operator
    plugin:
      name: sops-file
      env:
        - name: FILE
          value: oauth-secret.sops.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: tailscale
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
