apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cloudflared-secrets
  namespace: argocd
  annotations:
    # Deploy secrets BEFORE the main application (sync-wave 5)
    argocd.argoproj.io/sync-wave: "1"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/LorenzoDeBie/Homelab-new.git
    targetRevision: main
    path: kubernetes/core/cloudflared
    plugin:
      name: sops-file
      env:
        - name: PARAM_FILE
          value: credentials.sops.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: cloudflared
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
