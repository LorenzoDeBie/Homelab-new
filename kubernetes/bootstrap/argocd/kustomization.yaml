apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - namespace.yaml
  - httproute.yaml

# ArgoCD is installed via Helm during bootstrap
# This kustomization is for additional resources

# After initial install, ArgoCD manages itself via the core/argocd Application
