app-template:
  controllers:
    nzbget:
      containers:
        app:
          image:
            repository: lscr.io/linuxserver/nzbget
            tag: v26.2-ls254@sha256:87eba87ec46982b003ab1f54cfabd962c5c5e95cb9eb2069cbec7b6370d0784b
          env:
            TZ: Europe/Brussels
            PUID: "3001"
            PGID: "3001"
            NZBGET_PORT: &port 6789
          resources:
            requests:
              cpu: 50m
              memory: 256Mi
            limits:
              memory: 1Gi
          probes:
            liveness:
              enabled: true
            readiness:
              enabled: true
            startup:
              enabled: true
              spec:
                failureThreshold: 30
                periodSeconds: 5

  service:
    app:
      controller: nzbget
      ports:
        http:
          port: *port

  persistence:
    config:
      enabled: true
      type: persistentVolumeClaim
      storageClass: nfs-config
      accessMode: ReadWriteOnce
      size: 1Gi

    # Shared media volume for hardlink support with the *arr apps
    # Configure in NZBGet UI (Settings > Paths):
    #   MainDir: /media/usenet
    media:
      enabled: true
      type: persistentVolumeClaim
      existingClaim: media
      globalMounts:
        - path: /media
