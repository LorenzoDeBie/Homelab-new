app-template:
  controllers:
    overseerr:
      containers:
        app:
          image:
            repository: lscr.io/linuxserver/overseerr
            tag: 1.35.0@sha256:6108ed066d4a919c05251d9dab041c1e55e67ff7247e7b31be97b65ffcbaeeb1
          env:
            TZ: Europe/Brussels
            PUID: "3001"
            PGID: "3001"
            LOG_LEVEL: info
          resources:
            requests:
              cpu: 50m
              memory: 256Mi
            limits:
              memory: 1Gi
          probes:
            liveness:
              enabled: true
            readiness:
              enabled: true
            startup:
              enabled: true
              spec:
                failureThreshold: 30
                periodSeconds: 5
  
  service:
    app:
      controller: overseerr
      ports:
        http:
          port: 5055
  
  persistence:
    config:
      enabled: true
      type: persistentVolumeClaim
      storageClass: nfs-config
      accessMode: ReadWriteOnce
      size: 1Gi
      globalMounts:
        - path: /config
